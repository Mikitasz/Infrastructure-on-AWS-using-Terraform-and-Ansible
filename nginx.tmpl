events{}
http {
    upstream backend {

        server ${web_server};
    }

    server {
        listen 80;

        location / {
            
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}